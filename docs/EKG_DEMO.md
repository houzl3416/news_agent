# EKG (事件知识图谱) 独立测试文档

本文档介绍如何独立测试 EKG 模块，无需启动完整的 NEWS GT 系统。

## 快速开始

### 1. 环境准备

```bash
# 确保已在虚拟环境中
cd news_agent
source venv/bin/activate  # Windows: venv\Scripts\activate

# 确保已安装依赖
pip install -r requirements.txt
```

### 2. 运行 EKG Demo

```bash
python demos/ekg_demo.py
```

**运行时间**: 约 2-3 秒

**输出**: 控制台会打印详细的演示过程，展示 EKG 的所有核心功能。

### 3. 查看结果

运行后会在 `demos/` 目录生成：
- `ekg_demo.db` - SQLite 数据库文件

可以用任何 SQLite 工具打开查看：
```bash
# 使用 sqlite3 命令行工具
sqlite3 demos/ekg_demo.db

# 查看所有表
.tables

# 查看信源数据
SELECT * FROM sources;

# 查看事件数据
SELECT * FROM events;
```

## 演示场景说明

Demo 包含 7 个完整场景，演示 EKG 的核心功能：

### 场景 1: 基础操作

**演示内容**:
- 创建信源 (`@TechInsider`, `SEC官方`)
- 创建事件 (`OpenAI投资AMD传闻`)
- 创建声明 (`OpenAI将投资AMD 1000亿美元`)
- 创建证伪关系

**学习要点**:
- 理解 EKG 的基础数据结构
- 了解信源类型（官媒、自媒体等）
- 了解声明状态（待核实、已证实、已证伪）

**输出示例**:
```
📌 步骤 1: 创建信源
   创建信源: @TechInsider
   类型: social_media
   初始信誉分: 50

📌 步骤 2: 创建事件
   创建事件: E-001
   标题: OpenAI投资AMD传闻
   状态: developing

📌 步骤 3: 创建声明
   声明 1: OpenAI将投资AMD 1000亿美元
   信源: @TechInsider
   状态: pending
```

### 场景 2: 飞轮机制

**演示内容**:
- 模拟 `@TechInsider` 连续发布 3 条虚假消息
- 每次更新信誉分 -5
- 展示信誉分和准确率的动态变化

**学习要点**:
- **飞轮机制核心**：每次调查后，系统会"记住"信源的表现
- 信誉分会根据历史准确率动态调整
- 准确率 = 已验证声明数 / 总声明数

**输出示例**:
```
📌 模拟调查场景:
   场景: @TechInsider 连续发布3条虚假消息
   第 1 次: 信誉分 45 (准确率 0.0%)
   第 2 次: 信誉分 40 (准确率 0.0%)
   第 3 次: 信誉分 35 (准确率 0.0%)

📌 最终状态:
   信誉分: 50 → 35
   准确率: 0.0% → 0.0%
   总声明数: 4
   已证伪: 3

💡 飞轮效应:
   经过 4 次调查，系统已'记住'该信源不可靠
   下次遇到该信源时，可毫秒级预警！
```

### 场景 3: 信源声誉查询

**演示内容**:
- 模拟新调查开始前的 EKG 查询
- 毫秒级返回信源历史声誉
- 根据信誉分生成预警等级

**学习要点**:
- **这是飞轮的"读"操作**：利用历史数据快速预警
- 无需重新调查，直接查询即可
- 比竞争对手快 10+ 分钟

**输出示例**:
```
📌 场景: 用户提交新的可疑新闻
   新闻来源: @TechInsider
   内容: 'Google收购Adobe'

📌 系统立即查询 EKG（毫秒级）:
   ✅ 找到历史记录!
   信源: @TechInsider
   信誉分: 35
   历史准确率: 0.0%

   预警等级: 🔴 高度存疑
   建议: 该信源历史准确率仅 0.0%，建议谨慎对待
```

### 场景 4: 计算事件可信度

**演示内容**:
- 综合所有声明的状态
- 考虑信源信誉分
- 计算事件整体可信度评分

**学习要点**:
- 可信度计算公式：
  - 基准分 50
  - 已验证声明 +30%
  - 已证伪声明 -40%
  - 加权信源信誉分
- 评级标准：
  - >= 70: 高度可信
  - 40-70: 存疑
  - < 40: 不可信

**输出示例**:
```
📌 分析事件: E-001

   可信度评分: 26.5/100
   已验证声明: 1
   已证伪声明: 1
   总声明数: 2

   综合评级: ❌ 不可信
```

### 场景 5: 事件图谱可视化

**演示内容**:
- 生成事件的图谱数据（节点 + 边）
- 数据格式适合前端可视化库（D3.js, Cytoscape.js）

**学习要点**:
- 节点类型：事件、声明、信源
- 边类型：has_claim (事件包含声明), made_claim (信源发布声明)
- 可以用于前端展示信息传播路径

**输出示例**:
```
   节点数: 5
   边数: 4

   节点列表:
      - event: OpenAI投资AMD传闻
      - claim: OpenAI将投资AMD 1000亿...
        状态: refuted
      - source: @TechInsider
        信誉分: 35
      - claim: SEC未发现OpenAI-AMD交易...
        状态: pending
      - source: SEC官方
        信誉分: 50

   关系列表:
      - E-001 --[has_claim]--> claim-1
      - source-1 --[made_claim]--> claim-1
      - E-001 --[has_claim]--> claim-2
      - source-2 --[made_claim]--> claim-2
```

### 场景 6: 热门信源排行

**演示内容**:
- 按活跃度（声明数）排序
- 展示信源的信誉分和类型

**学习要点**:
- 可用于监控哪些信源最活跃
- 结合信誉分，识别高风险信源

**输出示例**:
```
📌 活跃度排行（按声明数）:
   1. @TechInsider
      类型: social_media
      信誉分: 35
      声明数: 4
   2. SEC官方
      类型: official_media
      信誉分: 50
      声明数: 1
```

### 场景 7: 完整调查流程

**演示内容**:
- 模拟从用户提交到调查完成的完整流程
- 展示 EKG 在流程中的作用

**学习要点**:
- 理解 EKG 如何集成到调查流程
- 理解飞轮机制的完整闭环

**输出示例**:
```
✅ 步骤1: 创建事件 E-002
✅ 步骤2: 溯源完成，原始信源: @SpaceNewsDaily
✅ 步骤3: EKG查询 - 新信源，信誉分 50
✅ 步骤4: 提取声明 - 'SpaceX将于2025年启动火星移民...'
✅ 步骤5: 核查完成 - 发现官方辟谣
✅ 步骤6: 更新声明状态为'已证伪'
✅ 步骤7: 更新信源信誉分 -5
✅ 步骤8: 更新事件状态为'已证伪'，可信度 33.3
✅ 步骤9: 保存调查历史 INV-002

🎯 调查完成！下次遇到 @SpaceNewsDaily 时，系统会立即预警。
```

## 数据库结构

EKG 使用以下数据表：

### 核心表

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| `sources` | 信源 | `id`, `name`, `type`, `credit_score` |
| `events` | 事件 | `id`, `title`, `status`, `credibility_score` |
| `claims` | 声明 | `id`, `text`, `source_id`, `event_id`, `status` |
| `entities` | 实体 | `id`, `name`, `type` |
| `artifacts` | 物料 | `id`, `type`, `url` |

### 关系表

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| `claim_refutations` | 证伪关系 | `refuting_claim_id`, `refuted_claim_id`, `confidence` |
| `investigation_history` | 调查历史 | `investigation_id`, `event_id`, `report`, `credibility_score` |

## 自定义测试

你可以修改 `demos/ekg_demo.py` 来测试自己的场景：

### 示例 1: 添加新的信源

```python
# 在 scenario_1_basic_operations 方法中添加

source3 = self.repo.find_or_create_source(
    name="路透社",
    source_type=SourceType.NEWS_OUTLET,
    url="https://www.reuters.com",
    description="国际权威新闻机构"
)
print(f"创建信源: {source3.name}")
```

### 示例 2: 测试信誉分更新

```python
# 模拟发布准确信息，信誉分上升

claim = self.repo.create_claim(
    text="准确的财报数据",
    source_id=source_id,
    status=ClaimStatus.VERIFIED  # 已验证
)

# 信誉分 +5
self.repo.update_source_credit_score(source_id, +5)
```

### 示例 3: 查询特定事件的所有声明

```python
claims = self.repo.get_claims_by_event("E-001")

for claim in claims:
    print(f"声明: {claim.text}")
    print(f"状态: {claim.status.value}")
    print(f"信源: {claim.source.name}")
```

## 常见问题

### Q1: 如何重置数据库？

```bash
# 删除数据库文件
rm demos/ekg_demo.db

# 重新运行 demo
python demos/ekg_demo.py
```

### Q2: 如何查看数据库内容？

**方法 1: 使用 sqlite3 命令行**
```bash
sqlite3 demos/ekg_demo.db

.tables
SELECT * FROM sources;
SELECT * FROM events;
.quit
```

**方法 2: 使用 SQLite GUI 工具**
- DB Browser for SQLite (https://sqlitebrowser.org/)
- 直接打开 `demos/ekg_demo.db`

**方法 3: 使用 Python 脚本**
```python
import sqlite3

conn = sqlite3.connect('demos/ekg_demo.db')
cursor = conn.cursor()

cursor.execute("SELECT * FROM sources")
for row in cursor.fetchall():
    print(row)

conn.close()
```

### Q3: 如何添加更多测试场景？

在 `demos/ekg_demo.py` 中添加新方法：

```python
def scenario_8_my_test(self):
    """场景8：我的自定义测试"""
    print("=" * 80)
    print("场景 8: 我的自定义测试")
    print("=" * 80)

    # 你的测试代码
    pass

# 在 run_all_scenarios 中调用
def run_all_scenarios(self):
    # ... 其他场景
    self.scenario_8_my_test()
```

### Q4: EKG 的性能如何？

**查询性能**:
- 信源声誉查询: < 1ms (SQLite)
- 事件可信度计算: < 10ms
- 图谱生成: < 50ms

**扩展性**:
- 当前使用 SQLite，适合 < 10万条记录
- 生产环境建议使用 PostgreSQL
- 未来可升级到专业图数据库（Neo4j）

### Q5: 如何集成到完整项目？

EKG 已经集成在主项目中：

```python
# 在 Agent 或 Orchestrator 中使用

from src.database import get_db
from src.ekg import EKGRepository, EKGGraphOps

# 获取 session
db = next(get_db())

# 创建 repository
repo = EKGRepository(db)

# 使用 EKG
source = repo.find_or_create_source(
    name="@example",
    source_type=SourceType.SOCIAL_MEDIA
)
```

## 核心概念总结

### 1. 飞轮机制

```
调查 1 → 更新信誉分 → 存入 EKG
   ↓
调查 2 → 查询 EKG (毫秒级) → 快速预警
   ↓
调查 3 → 更新信誉分 → EKG 更智能
   ↓
  ...越用越准
```

### 2. 信誉分规则

| 事件 | 信誉分变化 |
|------|-----------|
| 发布已验证声明 | +5 |
| 发布已证伪声明 | -5 |
| 发布未核实声明 | 0 |

**信誉分范围**: 0-100
- 初始值: 50
- 最低: 0
- 最高: 100

### 3. 可信度评分

公式：
```
基准分 = 50
已验证影响 = (已验证数 / 总数) * 30
已证伪影响 = (已证伪数 / 总数) * -40
信源影响 = 平均信源信誉分 * 0.3

最终分 = 基准分 * 0.7 + 已验证影响 + 已证伪影响 + 信源影响
```

## 下一步

1. **运行 Demo**: `python demos/ekg_demo.py`
2. **查看数据库**: 用 SQLite 工具打开 `demos/ekg_demo.db`
3. **修改测试**: 尝试添加自己的测试场景
4. **集成项目**: 在主项目的 Agent 中使用 EKG

## 技术支持

- 查看 [架构文档](architecture.md) 了解 EKG 设计
- 查看 [API 文档](api_spec.md) 了解如何通过 API 使用 EKG
- 查看源码注释获取详细实现细节

---

**祝测试顺利！🎉**
